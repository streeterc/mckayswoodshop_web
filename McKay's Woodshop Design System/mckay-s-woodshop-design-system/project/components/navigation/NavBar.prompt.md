One-sentence: the site-wide header; 76px tall, always forest green, never transparent-over-hero.

```jsx
<NavBar links={[{label:'Work',value:'work'}]} active="work" onNavigate={go}
  action={<Button variant="accent" size="sm">Request a quote</Button>} assetBase="../../assets" />
```
