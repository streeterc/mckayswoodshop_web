One-sentence: settings that take effect on the spot — notifications, shop-floor view options. For anything that needs saving, use Checkbox.

```jsx
<Switch label="Text me build updates" checked={v} onChange={e=>set(e.target.checked)} />
```
