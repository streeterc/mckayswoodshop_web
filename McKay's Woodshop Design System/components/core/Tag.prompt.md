One-sentence: descriptor chips for filtering a gallery or listing a piece's attributes (white oak, hand-rubbed oil, kitchen).

```jsx
<Tag selected onClick={toggle}>White oak</Tag>
<Tag onRemove={clear}>Live edge</Tag>
```

Selected state fills forest green. Use sentence case, not title case.
