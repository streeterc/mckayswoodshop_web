One-sentence: a status marker attached to a row, card or heading — never a standalone decoration.

```jsx
<Badge tone="success" dot>In the shop</Badge>
```

Tones map to meaning: success = in progress / on schedule, warning = waiting on client, danger = blocked, copper = featured, forest = shop-internal, neutral = everything else.
