One-sentence: icon-only affordances in toolbars, table rows and card corners.

```jsx
<IconButton label="Print work order" variant="outline"><Icon name="printer" /></IconButton>
```

Variants: ghost (default, in dense rows), outline (standalone), solid (forest green, for a single emphasised tool). 30/36/42px boxes — pair with 16–18px icons.
