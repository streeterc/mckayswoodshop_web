One-sentence: wrap every Input/Select/Textarea so labels, hints and errors stay consistent.

```jsx
<Field label="Room dimensions" hint="Rough measurements are fine." required>
  <Input placeholder="e.g. 12 ft × 9 ft" />
</Field>
```
