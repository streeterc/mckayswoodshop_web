One-sentence: text entry inside a Field.

```jsx
<Input prefix={<Icon name="search" size={16} />} placeholder="Search projects" />
```

Focus is a 1px forest border plus a 3px translucent copper ring — never a blue browser outline.
