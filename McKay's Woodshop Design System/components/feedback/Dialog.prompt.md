One-sentence: confirmations, short forms and approvals — at most one open at a time.

```jsx
<Dialog title="Approve the drawings?" description="We'll start milling once you approve."
  footer={<><Button variant="ghost" onClick={close}>Not yet</Button><Button onClick={ok}>Approve</Button></>} />
```
