One-sentence: shows where a job stands in the shop, on project pages and in the portal dashboard.

```jsx
<ProgressSteps steps={[{label:'Measure',meta:'Apr 2'},{label:'Design'},{label:'Mill'}]} current={1} />
```

Stage names come from the shop's real sequence — Measure, Design, Mill, Assemble, Finish, Install.
