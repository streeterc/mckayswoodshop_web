One-sentence: switches views within one page (project stages, gallery categories) — not for site-level navigation.

```jsx
<Tabs tabs={[{label:'Overview',value:'o'},{label:'Milestones',value:'m',count:6}]} value={t} onChange={setT} />
```
