One-sentence: the portal's primary navigation — 232px, deep forest, icon + label rows.

```jsx
<SideNav items={[{label:'Projects',icon:<Icon name="hammer" />,count:3}]} active="Projects" onNavigate={go} />
```
